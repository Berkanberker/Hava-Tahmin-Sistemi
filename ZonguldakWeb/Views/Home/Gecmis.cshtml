@model IEnumerable<ZonguldakWeb.Models.HavaDurumuKayit>

@{
    ViewData["Title"] = "Arşiv Kayıtları";
}

<div class="container" style="margin-top: 90px;">
    <div class="d-flex justify-content-between align-items-center mb-5">
        <div>
            <h1 class="display-6 fw-bold text-cyan mb-0">📜 Geçmiş Analiz Arşivi</h1>
            <p class="text-muted-light small mt-1">Veritabanındaki kayıtlı raporlar</p>
        </div>
        <a asp-action="Index" class="btn btn-outline-light glass-card px-4 py-2">⬅️ Komutaya Dön</a>
    </div>

    @if (!Model.Any())
    {
        <div class="glass-card p-5 text-center mt-5">
            <h1 class="display-1">📭</h1>
            <h4 class="text-muted-light mt-3">Henüz arşivde kayıt yok.</h4>
            <p class="text-muted-light">Ana sayfadan ilk taramayı başlatın.</p>
            <a asp-action="Index" class="btn btn-analiz-dev mt-3 py-2 px-4 fs-6">Analiz Yap</a>
        </div>
    }
    else
    {
        <div class="row">
            @foreach (var item in Model)
            {
                <div class="col-md-6 col-lg-4 mb-4">
                    <div class="glass-card h-100 d-flex flex-column hover-effect">
                        <div class="glass-card-header d-flex justify-content-between align-items-center">
                            <span class="text-cyan fw-bold">📅 @item.Tarih.ToString("dd.MM.yyyy")</span>
                            <span class="badge bg-blue bg-opacity-25 text-blue fw-light">@item.Tarih.ToString("HH:mm")</span>
                        </div>
                        
                        <div class="p-0 bg-dark bg-opacity-50 text-center position-relative overflow-hidden" style="height: 160px;">
                            <img src="@item.ResimYolu" class="img-fluid w-100 h-100 object-fit-cover opacity-75" alt="Grafik">
                            <div class="position-absolute bottom-0 start-0 w-100 p-1 bg-gradient-dark text-center text-white small" style="background: rgba(0,0,0,0.6);">
                                ID: #@item.Id
                            </div>
                        </div>

                        <div class="card-body flex-grow-1 p-3">
                            <p class="card-text text-muted-light" style="font-size: 0.9rem; line-height: 1.5;">
                                @if(!string.IsNullOrEmpty(item.RaporMetni) && item.RaporMetni.Length > 120)
                                {
                                    @item.RaporMetni.Substring(0, 120) <span>...</span>
                                }
                                else
                                {
                                    @item.RaporMetni
                                }
                            </p>
                        </div>
                        <div class="p-3 mt-auto border-top border-secondary border-opacity-25">
                             <button class="btn btn-outline-cyan w-100 rounded-pill btn-sm" type="button" data-bs-toggle="collapse" data-bs-target="#detay-@item.Id">
                                👁️ Detayı İncele
                            </button>
                        </div>
                        
                        <div class="collapse" id="detay-@item.Id">
                            <div class="card-body ai-terminal border-0 border-top border-purple rounded-bottom-4 p-3 text-start" style="font-size: 0.8rem; white-space: pre-wrap; max-height: 300px; overflow-y: auto;">
                                @item.RaporMetni
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
</div>